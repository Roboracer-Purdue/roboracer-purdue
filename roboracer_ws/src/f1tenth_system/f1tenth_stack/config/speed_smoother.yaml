speed_smoother:
  ros__parameters:
    in_topic: /ackermann_cmd_raw
    out_topic: /ackermann_cmd

    # Start conservative; raise later if you want faster stops.
    max_accel: 2.5
    max_decel: 1.0

    zero_speed_epsilon: 0.02
    rate_hz: 100.0